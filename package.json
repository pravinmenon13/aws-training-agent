{
  "name": "aws-training-agent",
  "version": "0.1.0",
  "description": "MCP server — AWS Training Calendar assistant",
  "type": "module",
  "scripts": {
    "build": "tsc"
  },
  "bin": {
    "aws-training-agent": "./build/index.js"
  },
  "files": ["build"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
